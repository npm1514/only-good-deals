# Only Good Deals

A curated affiliate deal discovery app built with Next.js and TypeScript.

## Run locally

```bash
npm install
npm run dev
```

Then visit http://localhost:3000.

## Current state

This starter uses sample deal data only. Affiliate URLs are placeholders. The next step is adding an admin workflow and product ingestion.
